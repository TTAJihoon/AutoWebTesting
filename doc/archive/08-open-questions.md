# 미해결 결정사항

> 모든 결정은 일자·결정자·근거와 함께 기록한다.

## 확정

### 2026-05-18 (1차 — 토론 직후)

| # | 항목 | 결정 |
|---|---|---|
| D1 | AI agent 역할 범위 | **TC 설계 + 자동 실행 + 결과 판정** (end-to-end) |
| D2 | ISO/IEC 25023 적용 범위 | 자동화 가능한 특성부터 모두 적용, 나머지는 Layer별 분류 |
| D3 | 산출물 1차 형태 | 설계 문서 (`doc/`) 우선, 구현은 별도 단계 |
| D4 | RAG 데이터 출처 | 익명화 전제로 활용 가능 |
| D5 | Skill 패키징 | 프로젝트 최상위 `SKILL.md` 1개 + 별도 배포 가능 sub-skill 분리 식별 |

### 2026-05-18 (2차 — Q1~Q8 응답 후)

| # | 항목 | 결정 |
|---|---|---|
| D6 | **신뢰 프레임** | **외부 인증 audit 아님.** TC 유효성은 시험원 자체 판단. "Audit trail" 표현은 *내부 검증성*으로 재정의. source_quote·traceability는 *외부 강제*가 아닌 *내부 reviewer 신뢰* 확보 수단. |
| D7 | 대상 제품군 (Phase 1) | **웹 제품** 우선. 향후 거의 모든 SW 제품군 (비중은 다르지만 범위 넓음) — 제품군별 specialized agent는 장기 로드맵. |
| D8 | 적용 표준 | **ISO/IEC 25023:2016 + 25051:2014** 확정. **25059는 논의 후 결정** (AI 포함 제품 증가로 향후 도입 필요성 인지). |
| D9 | TC ID 체계 | `TC-XXX-YYY` 형식. XXX = 기능(보통 소분류)의 일련번호, YYY = 같은 기능 내 TC 변형 일련번호. 예: `TC-001-001`, `TC-001-002`, `TC-002-001`, … (해석 세부는 후속 확인 필요) |
| D10 | TC 작성 양식 | 시험소 표준 layout 사전 제공. AI는 해당 양식으로만 출력. 양식 외 형식은 모두 거부. |
| D11 | 구현 stack 방향 | **기존 Claude Code 기반 시험방법 skill을 교체하지 않는다.** "어떻게 시켜야 더 높은 품질의 자동화 시험을 할 수 있는가"가 본 프로젝트의 본질. → AWT는 *기존 skill의 강화 layer*. |
| D12 | 병렬도 (Phase 1) | **1개 동시 시험을 정확히 동작**시키는 게 1차 목표. 10+ 동시 수행은 운영 안정화 이후. |
| D13 | 결함이력 RAG 사전 보유 데이터 | 제품명·업체명 없음. 기능명 + 결함내용만 보유. **추가 익명화 규칙은 D16에서 결정.** |

### 2026-05-18 (3차 — 추가 응답 후)

| # | 항목 | 결정 |
|---|---|---|
| D14 | 기존 skill 공유 | 사용자가 직접 제작한 skill이 아니므로 파일 공유 불가. **단, 시험방법 자체에 대해선 사용자가 인터뷰 응답 가능.** AWT는 인터뷰로 기존 방법론을 구조화한다. |
| D15 | 25059 도입 전략 | **안 2 (조건부 도입)**. 25023+25051은 즉시 적용, 25059는 AI 포함 제품에만 별도 sub-mapping. Layer 매트릭스에 *AI 제품 한정* 차원 추가. |
| D16 | 결함이력 익명화 수준 | **L1+L2는 자동 파이프라인, L3+L4는 수동 검토.** L5는 보류 (필요 시점에 도입). 기능명 도메인 사전 매핑은 비활성. |

### 2026-05-18 (4차 — TC ID 세부 + reviewer 시간)

| # | 항목 | 결정 |
|---|---|---|
| D17 | TC ID 계층 처리 | TC ID의 XXX는 **최하위 분류**(소분류든 중분류든 그 기능의 leaf)의 일련번호. 최하위가 아닌 상위 분류는 ID에 들어가지 않음. (필요 시 metadata에만 별도 기록) |
| D18 | YYY 자릿수 | **3자리 고정** (999까지 충분). 초과 시는 설계 재검토 신호. |
| D19 | Cross-feature TC ID | 별도 prefix 없음. 시나리오의 **주된 기능에 귀속**해서 일반 TC와 같은 `TC-XXX-YYY` 형식. |
| D20 | Reviewer 시간 budget | **TC 1개당 평균 30초~2분 (가변)**. Confidence에 따라 차등 적용 (자동 승인은 5초, 심층 확인은 2분 상한). |

### 2026-05-18 (5차 — 인터뷰 추가 답변)

| # | 항목 | 결정 |
|---|---|---|
| D21 | 25023 메트릭 % 산출 | **현재는 측정 자체를 하지 않음.** AWT가 자동 계산 도입 여지 있음 — Phase 2에서 결정 (내부 KPI용, 외부 보고서 포함 여부는 별도). |
| D22 | **Reviewer 워크플로 위치 변경** | **Reviewer Gate는 자동실행 *이전*에 위치한다.** 흐름: `TC 생성 → Reviewer Gate (검토·승인·수정) → 자동실행 → 최종 산출`. (기존 사후 검토 모델을 사전 게이트 모델로 재정의 — 사용자 명시적 요청) |
| D23 | 재시험 방식 | 시험원이 검토 후 변경한 내용을 적용하려면, **기존 TC 결과를 첨부해 재시험**한다. AWT는 이 메커니즘을 *Reviewer Gate 이후 실행 단계*의 통합 경로로 활용. (※ D25 pivot 이후 *AWT 내부 흐름의 한 단계*로 재해석.) |
| D24 | 실행 도구 | **Claude Code의 Playwright/브라우저 MCP** 사용. 별도 외부 자동화 프레임워크 도입 불요. AWT는 prompt + MCP 호출 레이어로 충분. |

### 2026-05-19 (8차 — Desktop App 아키텍처 재정의)

| # | 항목 | 결정 |
|---|---|---|
| **D37** | **프로덕션 런타임 재정의 (D25 보완)** | **D25의 "Claude Code Skill"은 개발·검증 환경(PoC)으로만 사용.** 프로덕션은 Python 데스크탑 앱으로 분리. Claude Code는 prompt 개발 및 PoC 실행 전용. 프로덕션 앱은 Claude Code 미설치 환경에서 동작. |
| **D38** | **LLM 연동 방식** | **추론이 필요한 단계만 Anthropic API를 stateless 호출.** 대화 히스토리 없음. 각 call은 독립된 요청. 정형화된 LLM Call Contract에 따라 입력 범위·출력 스키마를 사전 고정. (→ `doc/03-architecture/10-llm-call-contracts.md`) |
| **D39** | **Playwright 실행 방식** | **로컬 Python Playwright 라이브러리 사용.** Claude Code MCP 미사용. 설치 패키지에 `playwright install chromium` 포함. 브라우저 바이너리는 사용자 PC에 로컬 저장. |
| **D40** | **로그인 시스템** | **중앙 DB 서버 기반 인증.** 앱이 DB 서버에 직접 접속해 자격증명 검증. 처리(Playwright·API 호출)는 로컬 수행. 오프라인 시 로그인 불가. |
| **D41** | **토큰 최적화 원칙** | **① Per-leaf 처리:** TC 설계는 leaf 기능 1개씩 별도 호출. ② **최소 컨텍스트:** 해당 leaf 관련 매뉴얼 발췌문만 투입(전체 매뉴얼 미전송). ③ **JSON Schema 출력:** 자유 텍스트 금지, 사전 정의 스키마로만 응답. ④ **캐시:** 입력 미변경 시 LLM 재호출 금지. |
| **D42** | **API 키 관리** | **사용자별 개인 Anthropic API 키.** 앱 설정 화면에서 입력, 로컬 암호화 저장. 키는 서버로 전송되지 않음. |
| **D43** | **처리 파이프라인 오케스트레이션** | **로컬 Python 코드가 Stage 0~7 전체를 직접 orchestration.** Claude Code의 Tool 호출 흐름 불사용. Stage별 LLM Call Contract에 따라 Anthropic API를 직접 호출. |

### 2026-05-19 (7차 — 실제 OSS + 배포 방향 확정)

| # | 항목 | 결정 |
|---|---|---|
| **D32** | **Stage 0 — DOM 기반 기능 명세 초안 생성** | **필수 기능으로 확정.** 매뉴얼이 빈약하거나 없는 경우, AWT는 Playwright MCP로 DOM을 스캔해 기능 명세 초안(feature-spec-draft)을 자동 생성한다. 이 초안이 Stage 1 Ingest의 입력으로 투입되어 기존 파이프라인과 연결된다. |
| **D33** | **로그인 필요 페이지 DOM 스캔** | **필수 기능으로 확정.** Playwright 인증 시퀀스(아이디/비번 입력 → 세션 유지)를 Stage 0 내에 포함한다. 인증 정보는 실행 시점에 사용자 입력으로 받는다(하드코딩 금지). |
| **D34** | **배포 대상** | **Windows 사용자 전체.** PyInstaller로 단독 .exe 생성. Claude Code 미설치 환경에서도 실행 가능하도록 설계. |
| **D35** | **Windows 앱 구현 방식** | **B (Python GUI) 확정.** PyQt5 또는 PySide6 + Anthropic Python SDK 직접 호출. Electron(C)은 기각(빌드 복잡·JS 별도·바이너리 과대). |
| **D36** | **Stage 0 개발 우선순위** | **PoC (α→β→γ) 완료 후 1순위.** OSS 셋업은 PoC 완료 전 시작하지 않는다. |

### 2026-05-19 (6차 — Standalone Pivot)

| # | 항목 | 결정 |
|---|---|---|
| **D25** | **AWT의 구현 형태 — Pivot to Standalone** | AWT는 *기존 skill의 강화 layer*가 아니라 **사용자가 직접 실행하고 관찰할 수 있는 standalone Claude Code skill**로 구현된다. **D11을 대체.** 이유: 사용자가 기존 skill을 직접 실행해본 적 없으며 *"내가 만들면서 보고 싶다"*고 명시. Wrapper 모델은 user-job 불일치·외부 협조 의존·black box 문제로 부적합. 기존 skill은 *참고 자료*로만 유지. |
| D26 | Q-INT-5 (skill 분리 호출 가능성) | **해소 — N/A.** Standalone에서는 분리 자체가 불필요. |
| D27 | Q-PA-4 (prompt 충돌) | **해소 — N/A.** AWT의 prompt만 존재. |
| D28 | Q-PA-5 (Excel 컬럼 수용성) | **해소 — N/A.** AWT가 컬럼을 직접 정의. (TC 스키마 G1~G5는 AWT 자유 정의) |
| D29 | PoC 재정의 | 기존 PoC-1~6 → 새 **PoC-α / β / γ**로 단순화. (`06-poc-validation-plan.md` 재작성) |
| D30 | 기존 강화 5종(E1~E5)의 위상 | *외부 skill 강화 항목* → *AWT의 핵심 기능 5종*으로 재해석. 구현 우선순위·내용은 그대로. |
| D31 | PoC 대상 제품 | **간단한 한국어 오픈소스 게시판** (사용자 선택 2순위). AWT가 매뉴얼·기능리스트 합성 가능. 후보 식별은 다음 단계. |

## 미해결

| # | 질문 | 영향 | 결정 시점 | 비고 |
|---|---|---|---|---|
| Q4' | TC ID 체계 세부 — D17~D19로 대부분 해소, 남은 자질 시 추가 결정 | 작음 | 운영 시 발생 시 | D17~D19 |
| Q6 | Layer 2 검토자의 평균 가용 시간 (TC 1개당 몇 분) | 중 — Confidence threshold 정량 결정 | doc/04/04-confidence-score.md 작성 전 | **D20에서 *평균 30초~2분*으로 부분 해소** — 정밀 분포는 PoC 후 |
| ~~Q7''~~ | ~~기존 시험방법 인터뷰~~ | ~~매우 큼~~ | ~~doc/03 작성 전~~ | **해소 — 인터뷰 완료 (`doc/03/00-existing-method-interview.md`)** |
| Q14 | 시험원 reviewer의 결정권 범위 — AI 산출을 거부·수정할 때 책임 소재와 결재 라인 | 중 — Layer 2 워크플로 결정 | doc/02/05-curation-workflow.md 작성 전 | **부분 해소 — `05-curation-workflow.md` §6 (단일/2단 결재 옵션). 시험소 정책에 따른 최종 결정 잔여** |
| ~~Q-INT-5~~ | ~~기존 skill이 TC 생성만 단독 호출 가능한가~~ | ~~매우 큼~~ | ~~E1+E2 prompt 설계 직후~~ | **D26 해소 — Standalone pivot으로 N/A** |
| Q-PROD-1 | PoC용 한국어 오픈소스 게시판 *구체 후보* 결정 | 중 — PoC 시작점 | PoC 진입 전 | D31 위 |
| Q-MX-1~4 | 25023/25051/25059 매트릭스 정합성·시험소 분포 | 큼 | doc/02 검수 | `01-characteristics-matrix.md` §8 |
| Q-SCH-1~4 | TC 스키마 세부 | 작음 | E5 워크플로 / E3 설계 / 운영 | `04-tc-design-spec/01-tc-schema.md` §7 |
| Q-DF-1~4 | 데이터 흐름 구현 세부 | 작음 | 구현 단계 | `03-data-flow.md` §13 |
| Q-PA-1·2·3 | source_quote grep / INFERRED 임계 / 재호출 상한 | 중 — V2·V3·V4 정량 | PoC-α 종료 시 | 재정의된 PoC에서 검증 |

## 결정 누적 규칙

- 새 결정 → 위 표에 추가 (날짜·결정자·근거 명시)
- 기존 결정 변경 → 변경 이력 별도 섹션에 기록
- 미해결이 길어지는 항목은 *조건부 가정*으로 명시하고 영향 분석
