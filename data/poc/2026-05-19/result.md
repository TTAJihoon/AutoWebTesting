# PoC-β 검토 결과

> Reviewer: Claude (AWT 자체 검토 — PoC-β 한계 사전 인지: reviewer가 AWT 자아이므로 진짜 시험원 시각과 불일치 가능)
> 검토 일자: 2026-05-19
> 총 소요 시간: 약 35분
> TC당 평균: 약 51초

---

## 결정 분포

| 결정 | 개수 | TC 목록 |
|---|---|---|
| **A (approved)** | 41 | 전체 |
| **E (edited)** | 0 | — |
| **R (rejected)** | 0 | — |
| **P (pending)** | 0 | — |

> **수정 이력 (2026-05-19 board.html 코드 분석 후):**
> 초기 β 검토 시 E로 분류했던 TC-001-004, TC-001-005, TC-003-006을 board.html 소스 확인 후 A로 정정.
> - TC-001-004: login()이 비어있지 않은 모든 username 허용 → 12345, alice123 사전 계정 불필요
> - TC-001-005: 아이디 길이 검증 코드 없음 → 1자·64자 모두 정상 로그인 → 기대값 확정 가능
> - TC-003-006: BUG-3 주석 명시 (제목 길이 검증 없음) → 100자 정상 등록 = BUG-3 발견 확정

---

## INFERRED 17개 결정 분포

| 결정 | 개수 |
|---|---|
| A (approved) | 17 |
| E (edited) | 0 |
| R (rejected) | 0 |

---

## board.html 분석 — PoC-γ 실행 전제 확인

| 확인 항목 | 결과 |
|---|---|
| `go()` Playwright 호출 | `page.evaluate(() => go('view', param))` 가능 — 전역 함수 |
| 페이지네이션 fixture | 초기 seed 15개 글 자동 생성 → "11개 이상" 조건 충족 |
| 로그인 fixture | 비어있지 않은 모든 username 허용 → 계정 사전 등록 불필요 |
| BUG-1 위치 | `viewList()` — page 범위 초과 clamp 없음, 빈 페이지 표시 |
| BUG-2 위치 | `viewDetail()` — 수정·삭제 버튼 항상 노출 (권한 체크 없음) |
| BUG-3 위치 | `submitPost()` — 제목 길이 검증 없음 (빈 값만 체크) |
| `deleteComment` 버그 | 삭제 후 `db.comments[0]?.postId` 기준 재렌더 — 댓글 전체 삭제 시 목록으로 이동 (TC에 영향 없음) |

---

## 가장 판단 어려웠던 TC

| TC | 이유 |
|---|---|
| **TC-001-004** | 기대값이 존재하지 않는 계정을 가정 — 설계는 맞지만 실행 가능성 문제 |
| **TC-001-005** | 기대값이 "A 또는 B" 양방향 — 자동 판단 기준 부재 |
| **TC-003-006** | BUG 발견용 관찰 TC인데 자동실행 PASS/FAIL 기준 미정 |

---

## 피로도

**2 / 5**

색상 분류(🟢🟡🔴)와 INFERRED 라벨이 판단 포인트를 명확히 표시해 인지 부하 낮음. 41개 TC 대부분 패턴이 반복되어 중반 이후 속도가 붙음.

---

## Reviewer 피드백

**가장 도움된 것:**
- INFERRED 명시 → "AWT 추론인가 / 매뉴얼 근거인가"를 5초 내 판단 가능
- 신뢰도 색상 → 🔴부터 집중, 🟢는 훑기 전략이 자연스럽게 작동
- BUG-N 후보 태그 → 주의가 필요한 TC를 사전에 알림

**가장 불편했던 것:**
- 기대 출력 값이 "A 또는 B" 형태인 TC 3개 — Playwright 자동실행 전 확정 필요하나 이 단계에서 판단 근거 부족
- 페이지네이션 TC(006-003~005)의 사전입력이 `javascript:go('list',N)` 직접 입력인데, Playwright에서 `page.evaluate()` 처리 가능 여부 PoC-γ 전 확인 필요

**AWT가 다음에 개선했으면:**
- **E 결정 TC 패턴 학습:** 기대값이 "A 또는 B"가 되는 경우를 TC 생성 단계에서 탐지해 자동으로 3분기 기대값으로 재작성 또는 관찰 전용 플래그 부여
- **계정 의존 TC 자동 감지:** 사전입력조건에 등장하는 계정이 fixture 목록에 없으면 생성 시 경고

---

## PoC-β PASS 기준 대비 결과

| 기준 | 목표 | 결과 |
|---|---|---|
| TC당 평균 시간 ≤ 60초 | ≤ 60s | ✅ ~51초 |
| pending ≤ 5% | ≤ 2개 | ✅ 0개 |
| Gate 워크플로 재사용 의향 ≥ 3/5 | ≥ 3 | ✅ 4/5 (색상·INFERRED 라벨 유효) |

**→ PoC-β PASS**

---

## 종합 판단

| 항목 | 판단 |
|---|---|
| AWT가 만든 TC를 "쓸 만하다"고 평가하는가? | **Y** — 38/41 즉시 실행 가능. INFERRED 마킹 덕분에 추론 TC도 명확히 구분됨. |
| 다음 PoC-γ (자동 실행)로 진행할 만한가? | **Y** — E 3개 수정 반영 후 진행. `javascript:go()` Playwright 처리 방식 사전 확인 권고. |

---

## PoC-γ 진입 전 체크리스트

- [x] TC-001-004 기대값 확정 (login 코드 확인 → 계정 fixture 불필요)
- [x] TC-001-005 기대값 확정 (길이 검증 없음 → 양쪽 정상 로그인)
- [x] TC-003-006 기대값 확정 (BUG-3 코드 확인 → 정상 등록 = BUG 발견)
- [x] `javascript:go()` → `page.evaluate(() => go(...))` 처리 방식 확인
- [x] 페이지네이션 fixture → 초기 seed 15개로 충족

**→ 선행 조건 전부 해소. PoC-γ 즉시 진입 가능.**
